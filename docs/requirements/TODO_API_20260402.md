# api — TODO

> **Prerequisite:** `db-models` must be completed through at least Phase 3 before starting Phase 2 here.

## Phase 1: Project scaffolding

- [ ] Create `api/` directory with `src/` and `tests/` subdirectories
- [ ] Create `pyproject.toml` with dependencies: `fastapi`, `uvicorn`, `sqlalchemy`, `psycopg2-binary`, `pgvector`, `sentence-transformers`, `python-jose`, `loguru`
- [ ] Create `requirements.txt` or lock file
- [ ] Add dev dependencies: `pytest`, `pytest-cov`, `httpx`
- [ ] Add `db-models` as editable dependency (`pip install -e ../db-models`)
- [ ] Create `Makefile` with `test` target: `python -m pytest --cov=src --cov-report=term-missing`

> **Checkpoint:** Run `make test` (should pass with no tests collected). Commit referencing this file and Phase 1.

## Phase 2: Logging setup

- [ ] Create `src/logger.py` — centralized Loguru configuration per `LOGGING_PYTHON_V06.md`
- [ ] Validate required env vars at startup (`NAME_APP`, `RUN_ENVIRONMENT`, `PATH_TO_LOGS`)
- [ ] Fatal exit with explicit error if required vars are missing
- [ ] Configure sinks per `RUN_ENVIRONMENT` (development / testing / production)
- [ ] Enable rotation, retention, and `enqueue=True` for testing and production
- [ ] Install `sys.excepthook` for uncaught exception logging
- [ ] Create `.env.example` with all required environment variables

> **Checkpoint:** Run tests. If tests pass, check off completed tasks and commit referencing this file and Phase 2.

## Phase 3: FastAPI app and health check

- [ ] Create `src/app.py` — FastAPI application instance
- [ ] Initialize logger on app startup
- [ ] Implement `GET /` — returns HTML page with "Knowledge Base Service"
- [ ] Create top-level `main.py` that runs uvicorn with `src.app`
- [ ] Write tests for `GET /` endpoint using `TestClient`

> **Checkpoint:** Run tests. If tests pass, check off completed tasks and commit referencing this file and Phase 3.

## Phase 4: Authentication

- [ ] Create `src/auth.py` — Bearer token validation logic
- [ ] Hash incoming token and compare against `api_keys.key_hash`
- [ ] Return 401 on missing or invalid token
- [ ] Create FastAPI dependency for injecting auth into protected routes
- [ ] Write tests for valid token, invalid token, and missing header cases

> **Checkpoint:** Run tests. If tests pass, check off completed tasks and commit referencing this file and Phase 4.

## Phase 5: Token generation script

- [ ] Create `scripts/generate_token.py`
- [ ] Generate a secure random token
- [ ] Hash it and insert into `api_keys` with a human-readable name
- [ ] Print raw token to stdout for copying
- [ ] Write test to verify the script produces a valid, hashable token

> **Checkpoint:** Run tests. If tests pass, check off completed tasks and commit referencing this file and Phase 5.

## Phase 6: `POST /obsidian/matches` endpoint

- [ ] Create `src/routes/obsidian.py` with the matches route
- [ ] Accept JSON body: `{ "question": "..." }`
- [ ] Convert question to embedding using `sentence-transformers/all-MiniLM-L6-v2`
- [ ] Run pgvector cosine similarity search against `markdown_file_embeddings`
- [ ] Return top 5 matches with `id`, `file_name`, `file_path`, `score`, `snippet`
- [ ] Protect with Bearer token auth
- [ ] Write tests for valid query, missing auth, and empty results

> **Checkpoint:** Run tests. If tests pass, check off completed tasks and commit referencing this file and Phase 6.

## Phase 7: `GET /obsidian/file/{file_id}` endpoint

- [ ] Add file retrieval route to `src/routes/obsidian.py`
- [ ] Look up file record in `markdown_files` by `file_id`
- [ ] Read markdown file from disk using `file_path`
- [ ] Return `id`, `file_name`, `file_path`, `content`
- [ ] Return 404 if file record or file on disk not found
- [ ] Protect with Bearer token auth
- [ ] Write tests for valid file, missing file, and missing auth

> **Checkpoint:** Run tests. If tests pass, check off completed tasks and commit referencing this file and Phase 7.

## Phase 8: Systemd service file and deployment config

- [ ] Create `systemd/knowledge-base-api.service` (reference file, not installed)
- [ ] Verify paths match `limited_user` directory layout
- [ ] Document nginx reverse proxy configuration
- [ ] Document install and startup steps

> **Checkpoint:** Commit referencing this file and Phase 8.
